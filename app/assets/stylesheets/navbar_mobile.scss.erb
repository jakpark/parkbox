@import "colors"; 
@media only screen and (max-width: 768px) {
  nav {
    a {
      margin: 10px 15px;
      &.logo {
        margin: 10px;
      }
    
      &.button {
        padding: 10px;
        line-height: 1;
      }
    }
    .mobile-visible {
      display: block;
    }
  }
  
  .mobile-hidden {
    display: none;
  }
  
  
  .icon {
    max-width: 40px;
  }
  
  .btn-nav {
    margin: 10px;
  }
}